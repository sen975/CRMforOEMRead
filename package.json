{
  "name": "crm-for-oem",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c teal,amber \"npm run dev -w @oem-crm/api\" \"npm run dev -w @oem-crm/web\"",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:generate": "npm run prisma:generate -w @oem-crm/api",
    "db:migrate": "npm run prisma:migrate -w @oem-crm/api",
    "db:seed": "npm run prisma:seed -w @oem-crm/api",
    "db:import-eva-customers": "npm run prisma:import-eva-customers -w @oem-crm/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.3"
}
